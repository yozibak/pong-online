type Message {
  gameID: String!
  playerNumber: Int!
  ball: Ball!
  bar: Bar!
  frameCount: Int!
}

input BallInput {
  position: VectorInput!
  movement: VectorInput!
  missed: Boolean
}

type Ball {
  position: Vector!
  movement: Vector!
  missed: Boolean
}

input BarInput {
  position: VectorInput!
  command: String!
}

type Bar {
  position: Vector!
  command: String!
}

input VectorInput {
  x: Float!
  y: Float!
}

type Vector {
  x: Float!
  y: Float!
}

type Mutation {
  sendData(
    gameID: String!
    playerNumber: Int!
    ball: BallInput!
    bar: BarInput!
    frameCount: Int!
  ): Message
}

type Subscription {
  subscribeGame(gameID: String!, playerNumber: Int!): Message
    @aws_subscribe(mutations: ["sendData"])
}

type Query {
  # AWS AppSync convention to include this
  _: String
}
